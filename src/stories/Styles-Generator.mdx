import ColorGenerator from './utils/ColorGenerator.tsx'
import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Styles/Generator" />

# Color Palette Generator (Beta)

Create and customize color palettes for Tint color space. Preview your colors on
approximations of tint components and export the results as SCSS variables.

<ColorGenerator />
